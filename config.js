// Configuration for the PWA
const CONFIG = {
  // Replace this with your Google Apps Script web app URL
  // Deploy your Apps Script as a web app and paste the URL here
  API_URL: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec',
  
  // App information
  APP_NAME: 'Steelwool Africa Recruitment',
  APP_VERSION: '1.0.0',
  
  // You can also use a custom backend API if you prefer
  // Just make sure it handles the same endpoints as the Google Apps Script
};

// Instructions for setup:
// 1. Open your Google Apps Script project
// 2. Click "Deploy" > "New deployment"
// 3. Select "Web app" as deployment type
// 4. Set "Execute as" to "Me"
// 5. Set "Who has access" to "Anyone"
// 6. Click "Deploy"
// 7. Copy the web app URL and paste it above as API_URL
